<!doctype html>
<html lang="fr">

<head>
    <meta charset="utf-8" />
    <title>FixMyCity</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="/app.js" defer></script>
    <link rel="icon" href="/logo.jpg">
    <link rel="stylesheet" href="/style.css">
</head>

<body>
    <nav>
        <div class="left">
            <img src="/logo.jpg">
            <a href="/">FixMyCity</a>
        </div>
    
        <div class="right">
            <div class="search">
                <form action="#">
                    <input type="text" name="barre de Recherche" placeholder="Adresse">
                    <button type="submit"></button>
                </form>
            </div>

            <% if (username) { %>
                <p><%= username %></p>
            
            <% } else { %>
            <a href="/login">Se connecter</a>
            <% } %>
            
        </div>
    </nav>
    <div class="milieu">
        <button class="add">
            <a href="/report">Signaler</a>
        </button>
        <p class="date">Jour actuel : <span> <%= today %></span></p>
        <table class="signalements">
            <thead>
                <tr>
                    <th>Description</th>
                    <th>Adresse</th>
                    <th>Rapporté par</th>
                    <th>Date</th>
                </tr>
            </thead>
            <% if (description && adresse) { %>
                <tbody>
                    <td><%= description %></td>
                    <td><%= adresse %></td>
                    <td><%= username %></td>
                    <td><%= today %></td>
                </tbody>
            <% } %>
        </table>
    </div>
</body>

</html>